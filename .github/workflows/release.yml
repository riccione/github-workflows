name: Release

on:
  workflow_call:
    inputs:
      apt_packages:
        description: Additional apt packages to install on Ubuntu
        type: string
        default: ""

      bin_name:
        description: Name of the compiled binary
        required: true
        type: string

      use_upx:
        type: boolean
        default: true

      cargo_args:
        type: string
        default: "--locked"
      
      tag:
        required: true
        type: string

permissions:
  contents: read

jobs:
  build-release:
    name: ${{ matrix.build }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    # SINGLE SOURCE OF TRUTH: Define your binary name here
    env:
      BIN_NAME: ${{ inputs.bin_name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - build: linux-x86_64
            os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: linux-x86_64
            ext: ""
          - build: windows-x86_64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            platform: win-x86_64
            ext: ".exe"
          - build: macos-x86_64
            os: macos-latest
            target: x86_64-apple-darwin
            platform: macos-x86_64
            ext: ""
          - build: macos-arm64
            os: macos-latest
            target: aarch64-apple-darwin
            platform: macos-arm64
            ext: ""

    steps:
    - name: Checkout repository
      uses: actions/checkout@v7
      with:
        fetch-depth: 0

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}

    - name: Rust Cache
      uses: swatinem/rust-cache@v2
      with:
        key: ${{ matrix.target }}

    # --- Ubuntu-only system dependencies ---
    - name: Install Linux Dependencies
      if: matrix.os == 'ubuntu-latest' && inputs.apt_packages != ''
      run: |
        sudo apt-get update
        sudo apt-get install -y ${{ inputs.apt_packages }}

    - name: Build
      run: cargo build --release --target ${{ matrix.target }} ${{ inputs.cargo_args }}

    - name: Strip Symbols
      if: matrix.os != 'windows-latest'
      run: strip target/${{ matrix.target }}/release/${{ env.BIN_NAME }}

    - name: Run UPX
      if: matrix.os == 'ubuntu-latest' && inputs.use_upx
      uses: crazy-max/ghaction-upx@v4
      with:
        version: latest
        files: target/${{ matrix.target }}/release/${{ env.BIN_NAME }}
        args: --best --lzma

    - name: Set Changelog Base Reference
      if: matrix.os == 'ubuntu-latest'
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        # Try to find the latest production release tag via GitHub CLI
        LATEST_RELEASE=$(gh release view --json tagName --template '{{.tagName}}' 2>/dev/null || echo "")

        if [ -n "$LATEST_RELEASE" ]; then
          echo "CHANGELOG_BASE=$LATEST_RELEASE" >> $GITHUB_ENV
        else
          # Fallback to the root commit hash of your repository if no release exists
          echo "CHANGELOG_BASE=$(git rev-list --max-parents=0 HEAD)" >> $GITHUB_ENV
        fi

    - name: Generate Changelog
      id: changelog
      if: matrix.os == 'ubuntu-latest'
      uses: jaywcjlove/changelog-generator@main
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        base-ref: ${{ env.CHANGELOG_BASE }}

    - name: Upload binaries to release
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: target/${{ matrix.target }}/release/${{ env.BIN_NAME }}${{ matrix.ext }}
        asset_name: ${{ env.BIN_NAME }}_${{ matrix.platform }}${{ matrix.ext }}
        tag: ${{ inputs.tag }}
        overwrite: true
        prerelease: ${{ !startsWith(github.ref, 'refs/tags/') }}
        body: ${{ matrix.os == 'ubuntu-latest' && steps.changelog.outputs.changelog || null }}
